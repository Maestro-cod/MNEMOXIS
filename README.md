# MNEMOXIS | Spatial Intelligence OS

![Version](https://img.shields.io/badge/Version-1.2.0--Alpha-cyan)
![Tech](https://img.shields.io/badge/Tech-Three.js%20%7C%20Supabase%20%7C%20Tailwind-blue)

**MNEMOXIS** is a futuristic, 3D spatial productivity engine designed to transform abstract thoughts into a visual universe. By leveraging spatial memory, MNEMOXIS helps users manage high-complexity tasks without the cognitive load of traditional lists.

## 🌌 Core Features

- **Spatial Nebula Interface:** A fully interactive 3D universe where every task is a star.
- **Ambient Neural Voice Sync:** Real-time speech-to-spatial mapping. The system listens and generates task nodes automatically.
- **Cognitive Load Monitoring:** Live metrics tracking your mental bandwidth based on task density.
- **Biometric UI/UX:** A high-end "Glassmorphism" interface designed for focus and reduced anxiety.
- **Multi-Tier Orbit Pricing:** Scalable access for personal observers and industrial architects.

## 🚀 Deployment

### 1. Database Setup (Supabase)
Run the following SQL in your Supabase dashboard to initialize the spatial memory engine:

```sql
create table tasks (
  id bigint generated by default as identity primary key,
  user_id uuid references auth.users not null,
  text text not null,
  priority text default 'Medium',
  created_at timestamp with time zone default timezone('utc'::text, now()) not null
);

-- Enable Row Level Security
alter table tasks enable row level security;

-- Policies
create policy "Users can only see their own tasks" on tasks
  for select using (auth.uid() = user_id);

create policy "Users can insert their own tasks" on tasks
  for insert with check (auth.uid() = user_id);
```

### 2. Identity Configuration
1. Go to **Authentication > Providers** in Supabase.
2. Enable **Google** and follow the instructions to add your Google Client ID and Secret.
3. Add `stefanmaestro25@gmail.com` as an authorized user.

## 🛠️ Tech Stack
- **Frontend:** Next-gen HTML5/ES6+
- **Rendering:** Three.js (WebGL)
- **Styling:** Tailwind CSS (Custom Neural Preset)
- **Backend:** Supabase (Auth, DB, Realtime)

## 👤 Admin Access
- **Admin Email:** `stefanmaestro25@gmail.com`
- **Password:** *[Provided in private session]*

---
*Created by Maestro-cod & Mnemoxis AI Architecture.*
